#include <stdio.h>
#include <math.h>

int main()
{
    int i = 1, j = 5, n;
    float x = 4.1416, p = 1, y;
    x = x/i;
    y = pow((x-1),(j-2));
    printf("n = %d, sin (x) = %lf\n", i, y);

    return 0;
}
