/*
 * RIG for UVa 11016 - Square Counting
 */
#include<bits/stdc++.h>

using namespace std;

char line[82];

void generateString(int n)
{
    for (int i = 0; i<n; i++)
    {
        line[i] = ('A'+(rand()%26));
    }
    line[n] = 0;
}

int main()
{
//    freopen("output.txt","w",stdout);
    /* Variables used in the program */
    int i, n,m,t,j;
    int test_case;
    /* Initialize random seed: */
    srand (time(NULL));
    /* Make sure the number of test case range is from 1 through 25 */
    test_case = rand()%1;

    for(i = 0; i <= test_case; i++)
    {
        /* Make sure the number of point range is from 3 through 100 */
        t = (rand() % 98)+3;

        printf("%d\n", t);
        for(j = 0; j < t; j++)
        {
            n = rand() % 10001;
            m = rand() % 10001;
            printf("%d %d\n", n,m);
        }
    }
    printf("0\n");
    return 0;
}
